import { CultToken } from "~~/types/types";

export const BASE_URL = "http://localhost:3000";
export const BLOCKCHAIN_URL = "https://testnet.monadexplorer.com";

/** Live data only — use useApiTokens / Mongo. */
export const tokens: CultToken[] = [];
