"use client";

import React, { useRef } from "react";
import { useGSAP } from "@gsap/react";
import { horizontalLoop } from "~~/lib/gsap/utils";

const Banner = () => {
  // Banner is disabled - always return null
  return null;
};

export default Banner;
